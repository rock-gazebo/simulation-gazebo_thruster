package rock_thruster.msgs;

message Raw
{
    optional string         name = 1;
    optional double         raw = 2;
}

message Effort
{
    optional string         name = 1;
    optional double         effort = 2;
}

message Joints
{
    repeated Raw            raw = 1;
    repeated Effort         effort = 2;
}
